import React from 'react';
import ParentHome from '../../pages/Parent/BottomTabs/ParentHome';

const ParentRoutes = () => {
  // ParentHome already contains its own bottom tab navigator
  return <ParentHome />;
};

export default ParentRoutes;